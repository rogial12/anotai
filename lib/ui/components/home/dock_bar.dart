import 'package:flutter/material.dart';

// Dock flutuante na base da HomeView com 3 abas: Anotações, Arquivo, Lixeira.
// TODO: extrair de home_view.dart quando o design for implementado.
class DockBar extends StatelessWidget {
  const DockBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder sem impacto visual
  }
}
