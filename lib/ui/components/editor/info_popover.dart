import 'package:flutter/material.dart';

// Popover com informações da nota: data de criação, contagem de palavras, etc.
// TODO: extrair de editor_view.dart quando o design for implementado.
class InfoPopover extends StatelessWidget {
  const InfoPopover({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder sem impacto visual
  }
}
