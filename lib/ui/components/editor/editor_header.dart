import 'package:flutter/material.dart';

// Header sticky da EditorView: status de salvamento e botões de ação.
// TODO: extrair de editor_view.dart quando o design for implementado.
class EditorHeader extends StatelessWidget {
  const EditorHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder sem impacto visual
  }
}
